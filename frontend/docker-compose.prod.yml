services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile.prod
    image: cliniq-frontend:latest
    restart: unless-stopped
    env_file: .env
    ports:
      - "3000:3000"
    networks:
      - cliniq_network

networks:
  cliniq_network:
    external: true
    name: cliniq_network
